/**
 * Italia PagoPA Proxy
 * Cittadinanza Digitale PagoPA services
 */
export declare const CONFIG: {
    CONTROLLER: {
        PORT: number;
        HOST: string;
        ROUTES: {
            NOTIFICATION_REGISTER: string;
        };
    };
    PAGOPA: {
        HOST: string;
        PORT: number;
        SERVICES: {
            NOTIFICATION_REGISTER: string;
        };
    };
};
